/* js/court.js — stub Court / characters module */
(function (global) {
  'use strict';

  function onShow() {
    /* no-op until full court UI is ported */
  }

  global.BloodCourt = {
    onShow: onShow
  };
})(typeof window !== 'undefined' ? window : this);
